# ⚠️ SECURITY WARNING: The key shard key-file listed below must be generated via
# Shamir's Secret Sharing from the master consensus private key. Do NOT copy the
# same key material to peer cosigner nodes. Never reuse test keys for production.
threshold = 2

[cosigner]
node-id = 1
listen-addr = "0.0.0.0:2222"

peers = [
    { id = 1, address = "horcrux-signer-0.horcrux-service.sovereign-chain.svc:2222" },
    { id = 2, address = "horcrux-signer-1.horcrux-service.sovereign-chain.svc:2222" },
    { id = 3, address = "horcrux-signer-2.horcrux-service.sovereign-chain.svc:2222" }
]

[chain]
chain-id = "sovereign-1"
key-file = "/secrets/share-1.key"
state-file = "/data/state.json"
double-sign-protection = true
timeout-ms = 1000
