apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db-read
  labels:
    app: db-read
spec:
  serviceName: db-read
  replicas: 1
  selector:
    matchLabels:
      app: db-read
  template:
    metadata:
      labels:
        app: db-read
    spec:
      containers:
        - name: db-read
          image: timescale/timescaledb-ha:pg16
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: "sovereign_read"
            - name: POSTGRES_USER
              value: "api_reader"
            - name: POSTGRES_PASSWORD
              value: "sovereign_read_pwd"
          command: ["postgres"]
          args:
            - "-c"
            - "wal_level=replica"
            - "-c"
            - "archive_mode=on"
            - "-c"
            - "archive_command=test ! -f /var/lib/postgresql/archive/%f && cp %p /var/lib/postgresql/archive/%f"
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 1000m
              memory: 2Gi
          volumeMounts:
            - name: db-read-data
              mountPath: /var/lib/postgresql/data
            - name: pg-read-archive
              mountPath: /var/lib/postgresql/archive
            - name: schemas-volume
              mountPath: /docker-entrypoint-initdb.d/000001_init_read.sql
              subPath: 000001_init_read.sql
            - name: schemas-volume
              mountPath: /docker-entrypoint-initdb.d/000002_timescale.sql
              subPath: 000002_timescale.sql
            - name: schemas-volume
              mountPath: /docker-entrypoint-initdb.d/000003_percentile.sql
              subPath: 000003_percentile.sql
      volumes:
        - name: schemas-volume
          configMap:
            name: db-schemas-config
        - name: pg-read-archive
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: db-read-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: db-read
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: db-read
